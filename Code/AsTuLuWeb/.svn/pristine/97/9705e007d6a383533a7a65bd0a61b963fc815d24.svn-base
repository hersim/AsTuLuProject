using System;
using System.Collections.Generic;
using AsTuLuWebProject.Database;

namespace AsTuLuWebProject.Models.Interfaces
{
    public interface IBookRepository : IDisposable 
    {
        List<Book> ListBooks();

        List<Book> ListHighestRatingBooks();

        List<Book> ListTwentyLatestBooks(int pageNumber, string firstOption, string secondOption);

        List<Book> ListRandomBooks(int pageNumber);

        Book GetBookById(int bookId, bool hasDisposed);

        BookByUser GetRelationshipForBook(UserAccount userToShow, Book bookToShow);

        bool CheckIfBookPresent(BookModel bookToCheck);

        bool CheckIfBookPresent(Book bookToCheck);

        bool HasDisposed();

        void Save();
        
    }
}